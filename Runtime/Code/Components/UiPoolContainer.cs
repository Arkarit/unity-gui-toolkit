using UnityEngine;

namespace GuiToolkit
{
	// This is just a marker for a container which only contains pooled objects, which are
	// automatically added to the pool when destroyed.
	public class UiPoolContainer : MonoBehaviour
	{
	}
}